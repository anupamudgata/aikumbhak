{
  "name": "kumbhak",
  "version": "1.0.0",
  "private": true,
  "description": "Kumbhak Breathing Studio — web app + serverless monetization (Razorpay lifetime unlock).",
  "engines": { "node": ">=18" },
  "scripts": {
    "build:www": "node tools/build-www.mjs",
    "cap:sync": "npm run build:www && npx cap sync",
    "cap:ios": "npm run cap:sync && npx cap open ios",
    "cap:android": "npm run cap:sync && npx cap open android"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "@revenuecat/purchases-capacitor": "^8.0.0",
    "@supabase/supabase-js": "^2.45.0",
    "razorpay": "^2.9.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
